1. Install dependencies

   ```bash
   npm install
   ```

2. Start the app

   ```bash
   npx expo start
   ```

4. 
run the backend and change the ip address in HandwritingCanvas.tsx to the ip address of your backend